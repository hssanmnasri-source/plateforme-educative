rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow everyone to read files (for displaying course thumbnails)
    match /{allPaths=**} {
      allow read;
      
      // Allow authenticated users to upload and delete files
      allow write: if request.auth != null;
    }
  }
}
